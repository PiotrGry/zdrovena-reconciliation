name: PR Validate — develop → main

# Every PR targeting main runs the fast quality gate plus the staging smoke/E2E
# suite. The full suite is intentionally kept here, not on every develop PR,
# because it deploys staging resources and runs browser workflows.
#
# Path filtering: Skip for infra-only changes (terraform.yml handles those).

on:
  pull_request:
    branches: [main]
    paths:
      - "zdrovena/**"
      - "tests/**"
      - "scripts/**"
      - "contracts/**"
      - "pyproject.toml"
      - "Dockerfile"
      - "e2e/**"
      - "frontend/**"
      - ".sops.yaml"
      - ".sops.yaml.example"
      - ".env.local.sops"
      - "docs/devops/**"
      - ".github/workflows/pr-validate.yml"
      - ".github/workflows/_quality-gate.yml"
      - ".github/workflows/_full-test-suite.yml"
      # infra/** intentionally omitted — terraform.yml runs plan for those
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write
  id-token: write
  security-events: write  # required by _quality-gate.yml (gitleaks/trivy SARIF upload)

concurrency:
  group: pr-validate-${{ github.head_ref || github.ref_name }}
  cancel-in-progress: true

jobs:
  quality-gate:
    name: Quality gate
    uses: ./.github/workflows/_quality-gate.yml
    secrets: inherit

  full-test-suite:
    name: Full staging smoke + E2E
    needs: [quality-gate]
    uses: ./.github/workflows/_full-test-suite.yml
    with:
      backend_changed: ${{ needs.quality-gate.outputs.backend }}
      frontend_changed: ${{ needs.quality-gate.outputs.frontend }}
    secrets: inherit

  ci-gate:
    name: CI Gate
    if: always()
    runs-on: ubuntu-latest
    timeout-minutes: 2
    needs: [quality-gate, full-test-suite]
    steps:
      - name: Check all jobs passed
        run: |
          results='${{ toJSON(needs.*.result) }}'
          echo "Job results: $results"
          if echo "$results" | grep -qE '"failure"|"cancelled"'; then
            echo "One or more CI jobs failed."
            exit 1
          fi
          echo "All CI jobs passed — ready to merge."

{
  "name": "zdrovena-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright E2E tests for zdrovena dashboard",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
